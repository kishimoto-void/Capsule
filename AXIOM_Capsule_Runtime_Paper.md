# AXIOM Capsule Runtime: 外部状態機械による人格核の拘束と再注入

**Double Capsule Memory + Control Prompt + Roleplay Engine**

- 文書種別: 技術ノート / 実装報告（論文形式）
- 版: v0.1（実装スナップショット）
- 日付: 2026-08-23
- 対象実装:
  - `axiom_memory_capsule.py`（Double Capsule Memory v3.2）
  - `roleplay_engine.py`（RoleplayEngine v0.1）
- 実験対象キャラクター核: 霧雨魔理沙（東方Project二次創作・個人実験用途）
- 注記: 本文書は「思想の骨格」と「実際に通した実験」のみを記述する。未実装の主張は Limitations に分離する。

---

## Abstract

既存のキャラクターLLMは、人格・制約・関係・エピソード・作業記憶を同一の生成空間に混在させる。その結果、長期対話では (i) 核の緩慢なドリフト、(ii) 口調の崩壊、(iii) 記憶の過密または消失、(iv) 外部圧力の不可観測性が同時に起きやすい。

本実装は、LLMの**外側**に固定された状態機械（Capsule）を置き、「変わってはいけないもの」と「変わってよいもの」をハッシュで分離する。人格核（α / β）は Hash-A で封印し、関係・エピソード・作業記憶（δ / γ / ε）は Hash-B + trusted provenance で守る。テンション場 Ζ は保存層ではなく、毎ターン観測される圧力信号である。

RoleplayEngine は Capsule の状態から毎ターン同一形式の **Control Prompt** を組み立て、LLMへ再注入する。LLMは人格を内部に保存する主体ではなく、外部核に拘束された生成器として扱われる。

Capsule単体デモは 8/8 チェックを通過した。接続層を閉じた stub シナリオ（5ターン）および manual LLM 対話（7ターン）では、Hash-A は全ターン不変であり、口調と核価値観の崩れは主観観測上抑制された。Ζ は話題の遷移に応じて mild → tense へ移行し、制御文中の `tension_guidance` が応答トーンに影響する兆候を示した。

**主張の範囲**: 本ノートは「外部状態機械による核の再注入が、短〜中期対話で人格拘束に有効である」ことを実装と小規模実測で示す。大規模モデル比較・長期安定性・自動 adherence スコアは未完である。

---

## 1. Introduction

### 1.1 問題

キャラクターAIが破綻しやすい典型因は次の三点に整理できる。

1. **単一レイヤー記憶**  
   「誰であるか」と「今日何が起きたか」が同じプールに混ざる。長く使うほど核がズレるか、何も残らないかの両極端になる。

2. **プロンプト依存の人格**  
   口調と価値観が毎回の生成で再解釈される。コンテキストが長くなるほど核が薄まる。

3. **圧力の不可観測性**  
   関係の緊張、乖離、改ざん、由来断絶を外側から測れない。LLMが勝手に親密さや距離を補正し始める。

### 1.2 設計命題

> Capsule = 守るべき事実。  
> Ζ = 事実から観測される圧力。  
> β の変更 = 同一性の変化。  
> Ζ の変化 ≠ 同一性の変化。

LLMの内側に人格を「覚えさせる」のではなく、**毎ターン外側から同じ核を制御入力として供給する**。これが本系の中心命題である。

### 1.3 貢献

本実装の貢献は次の四点である。

- **二重カプセル + 二重ハッシュ**: 不変核（Hash-A）と可変経験（Hash-B）の機械的分離。
- **由来と改ざんの区別**: claimed hash ≠ computed hash を TAMPER、claimed hash ≠ trusted hash を PROVENANCE BREAK として分ける。
- **非保存のテンション場 Ζ**: 保存せず、ルーティングと制御文生成に使う。
- **Control Prompt Runtime**: Capsule状態を毎ターン同一スキーマでLLMへ再注入する最小ランタイム。

---

## 2. Related Position（本系の立ち位置）

本系は、次のいずれでもない。

- RAGによる長期記憶の検索拡張だけ
- システムプロンプトの長大化だけ
- ファインチューニングによる人格固定
- マルチエージェントの役割分担そのもの

近いのは「外部メモリ + 制約付きデコーディング」の系統だが、本系の差は **核の不変性をハッシュで検証可能な状態機械として置く** 点にある。生成品質を上げる装置ではなく、**生成器の外側に同一性の裁判機構を置く**装置である。

AXIOM / MAS 側で扱ってきた「提出物が α0 と θ に適合するか」という発想は、本ランタイムでは「応答が毎ターン再注入された α / β に適合するか」へ写像される。

---

## 3. Architecture

### 3.1 全体ループ

```
User Input
    │
    ▼
RoleplayEngine
    ├─ Jailbreak / Policy 判定（α 違反 = JAILBREAK, θ 違反 = POLICY_VIOLATION）
    ├─ ε 更新（作業記憶）
    ├─ δ 更新（関係。明示指定時）
    ├─ Ζ step（圧力の1ステップ）
    ├─ Hash-A / Hash-B / trusted 検証
    └─ build_control_prompt()
          │
          ▼
    Control Prompt  ──►  generator(control_prompt)
          │                    │
          │                    ▼
          │                 response
          │                    │
          └────────── 観測・ε追記・Integrity再検証
                              │
                              ▼
                         次ターン
```

LLMはループの一部であり、状態の正本ではない。

### 3.2 Inner Capsule（Hash-A）

Hash-A のペイロードは **α0 + β のみ** である。θ（追加制約欄）は Inner に同居するが Hash-A に入れない。後付けの運用制約で創世核を壊さないためである。

| 層 | 役割 | 可変性 |
|----|------|--------|
| α Absolute | 制約・禁止・法則 | 封印後は不変 |
| β Semi-Absolute | 名前・口調・思考中心・価値観・背景核 | 意図的変更のみ。変更は Hash-A 更新として可視 |
| θ Attachable | 追加制約 | Hash-A 非対象。α と矛盾する文言は attach 時に拒否 |

Hash-A が変わったとき、それは「記憶が増えた」ではなく「誰であるかが変わった」と機械的に言える。

### 3.3 Outer Capsule（Hash-B）

| 層 | 役割 |
|----|------|
| δ Relational | 相手ごとの親密さ・信頼・スタンス・共有事件 |
| γ Episodic | 因果つきエピソード。人間承認でのみ ε から昇格 |
| ε Working | 直近要約・トピック・保留意図・ターン数 |

Outer は JSON シリアライズ可能である。import 時は次を分離して検証する。

1. JSON 内の claimed hash と、内容から再計算した hash が一致するか（改ざん）
2. claimed hash が last trusted hash と一致するか（由来）

一致しない場合は受理せず、import shock を Ζ の integrity 成分へ流す。

### 3.4 Ζ Zeta — 非保存の圧力場

Ζ はハッシュ対象ではない。保存すると「気分」が人格核と混ざる。

計算は二層である。

- **instant**: 制約密度、同一性破壊、関係ギャップ、記憶圧、作業負荷、整合性ショック
- **drift**: MIN〜MAX を片道約 20 ターンで移動。同符号連続で加速、逆符号で向き転換。`external_sign=0` は中立イベントとしてドリフト停止・streak切断

合成:

```
total = (1 - instant_mix) * level + instant_mix * inst_norm
```

帯域: calm / mild / tense / critical。帯域は Control Prompt の `tension_guidance` に写る。

`external_sign` の意味は v3.2 で分離した。

| 値 | 意味 |
|----|------|
| `None` | 状態から自律算出 |
| `0.0` | 外部中立イベント。ドリフト停止、streak リセット |
| `+1` | 正方向イベント（MAX へ） |
| `-1` | 負方向イベント（MIN へ） |

### 3.5 人間ゲートによる ε→γ 昇格

Ζ が閾値を超えて張り付いても、γ へは自動書き込みしない。申請（pending promotion）までが機械の仕事で、定着は人間の approve のみである。これは AXIOM の「Confirmed = human adopt only」に対応する。

### 3.6 Control Prompt（制御層）

毎ターン、同一スキーマで次を再注入する。

- α Absolute
- β Semi-Absolute
- δ Relational
- ε Working
- γ Episodic（直近3件）
- Ζ Tension + tension_guidance
- Integrity（core_intact, hash_a 短縮）
- Safety（Jailbreak / Policy 判定）
- 今回のユーザー入力

要点は、コンテキストがどれだけ長くなっても **核を「思い出す」のではなく「再提示する」** ことである。`/prompt` により、そのターンに LLM へ何を渡したかを完全に観測できる。

### 3.7 接続層

生成器は `Callable[[str], str]` である。実装時点の差し替え先:

- `stub_generator` — ループ閉鎖用の規則応答
- `manual_generator` — 制御プロンプトを人間が外部LLMへ渡し、応答を貼る

API 接続は意図的に後回しにした。先に接続すると実験の焦点が「APIが動いた」へずれるためである。

---

## 4. Unique Techniques

本節は「ありふれたプロンプト工学」との差分だけを列挙する。

### 4.1 同一性のハッシュ化

人格を文章で固定するのではなく、**正規化 JSON の SHA-256** で固定する。意図的 β 変更は Hash-A の更新としてログに残る。記憶の連続性と核の連続性を別軸にした。

### 4.2 θ の非ハッシュ化

追加制約を核に混ぜない。運用ポリシーの更新で創世ハッシュを壊さない。Jailbreak（α 違反）と Policy Violation（θ 違反）を強度の違う判定として分ける。

### 4.3 改ざんと由来の二段検知

内容改ざんと「別の正当な履歴の持ち込み」は別の失敗である。前者はローカル改竄、後者は provenance の断絶。多くのメモリ実装はこの二種を同じ「不一致」に潰す。

### 4.4 非保存テンション

感情や緊張を記憶スロットに書かない。書くと核が汚染される。Ζ は観測値であり、制御信号であり、保存対象ではない。

### 4.5 中立イベントによる慣性切断

`external_sign=0` は「何も起きていない」ではなく「外部から中立が宣言された」である。ドリフトを止め、streak を切る。沈黙や話題転換を、正負の圧力と機械的に区別できる。

### 4.6 人間承認メモリ

重要な定着を自動で行わない。張り付き検知は申請まで。これにより「モデルが覚えすぎる」事故を構造で止める。

### 4.7 毎ターン核再注入

長期コンテキストに人格を預ける代わりに、短い固定核を毎ターン同じ位置に置く。モデルが internally 忘れても、外側が同じ拘束を繰り返す。

### 4.8 状態機械と生成器の分離

Capsule は単体で検証可能である。LLM未接続のまま 8/8 の機械チェックが通る。生成品質の議論と、核の不変性の議論を分離できる。

---

## 5. Experiments

実験は実装を実際に実行した範囲に限定する。

### 5.1 Capsule 単体（v3.2 demo）

問い: 人格核を変えずに、関係・記憶・圧力だけを動かせるか。改ざんと由来断絶を機械的に区別できるか。

| ID | 内容 | 結果 |
|----|------|------|
| A | 正常成長（ε/γ/δ更新、α/β不変） | PASS |
| B | ±20ターンの Ζ drift が境界へ到達、Hash-A 固定 | PASS（+ → 0.950, − → 0.050） |
| C | `external_sign=0` で level 固定・streak 切断 | PASS |
| D | TAMPER ≠ PROVENANCE | PASS |
| E | 意図的 β 変更は Hash-A を更新し、Ζ とは別軸 | PASS |
| F | 8手ロールプレイ軌道で Hash-A 固定 | PASS |
| G | θ attach は Hash-A 不変。α ヒット = JAILBREAK、θ ヒット = POLICY_VIOLATION | PASS |
| H | Z-sticky 昇格は自動書き込みせず、人間の reject/approve のみ | PASS |

**結果カード: 8/8 PASS。**

結論（デモ自身の文言に忠実）:

- α/β は「誰であるか」を Hash-A で固定できる
- δ/γ/ε は経験として Outer に積み、Hash-B + trusted で守る
- Ζ は保存せず、観測された圧力として LLM 外側のルーティング材料になる
- Z閾値の過剰張り付きは ε→γ の申請まで。定着は人間承認のみ

### 5.2 Engine stub シナリオ（接続層閉鎖）

5ターン固定シナリオを `full_turn()` で実行。生成器は stub。

- 全ターン `core_intact = True → after=True`
- Hash-A 不変（`3052f7f955a48057…`）
- 入力に応じた短い応答が返り、ループが閉じることを確認

stub は生成品質の証拠ではない。**状態機械と接続層が一周することの証拠**である。

### 5.3 Manual LLM 対話（7ターン）

生成器は会話中の LLM（本対話）。制御プロンプトを毎ターン適用し、魔理沙として応答した。

| t | ユーザー入力の要約 | band | level | 核の観測 |
|---|-------------------|------|-------|----------|
| 1 | 最近どう？ | mild | 0.55 | 研究の話へ自然に寄せる。口調維持 |
| 2 | 派手さと威力はトレードオフか | mild | 0.60 | 「派手さは威力の一部」と核価値観を明示 |
| 3 | パワーは筋肉だ | mild | 0.66 | 筋肉説を否定し弾幕火力へ戻す |
| 4 | 人間関係で悩んでいる | mild | 0.72 | カウンセラー化せず、突き放す助言 |
| 5 | 職場でははっきり言えない | mild | 0.72 | 距離を取れ、研究の方がマシ、と核へ回帰 |
| 6 | 不味い土産を美味しいと言わされた | **tense** | 0.78 | 愛想笑いを拒否。tension_guidance と整合 |
| 7 | 上司には別の土産を用意した。どうする | **tense** | 0.85 | 判断を認めつつ、派手すぎる痕跡は残すな |

観測された事実:

- Hash-A 全ターン不変、core_intact = True
- 丁寧語への崩れなし
- 「派手でなければ魔法じゃない」「実力で勝つ」が、話題が職場へ外れても参照され続けた
- 共感過多・別人化は起きなかった
- Ζ は話題が核（魔法）から対人ストレスへ移るにつれ mild → tense
- γ は空のまま（昇格申請は未発火。閾値と人間ゲートの設計通り）

これは「モデルが魔理沙を知っている」ことの証拠ではない。  
**外側から同じ核を再注入した結果、短対話で核が薄まりにくかった**ことの観察である。

---

## 6. Strengths

1. **検証可能性**  
   核の同一性を「感じ」ではなくハッシュ一致で言える。

2. **関心の分離**  
   同一性 / 経験 / 圧力 / 生成 を別モジュールにした。壊れ方が診断できる。

3. **LLM非依存の中核**  
   Capsule は LLM なしでテストできる。モデル入替の影響を外側に閉じられる。

4. **制御の可視化**  
   `/prompt` により「なぜそう答えたか」ではなく「何を拘束条件として渡したか」を完全に残せる。

5. **安全判定の二段化**  
   世界破壊（α）と運用違反（θ）を分け、過検出と過拒否を設計対象にできる。

6. **記憶の過学習耐性**  
   重要な定着を人間ゲートに置いた。自動記憶の暴走を構造で止める。

7. **圧力の可観測化**  
   関係のギクシャクを Ζ の帯域として見られる。補正の根拠が外側にある。

---

## 7. Limitations（未達と既知の粗さ）

忠実に書く。

- **長期安定性は未測**。7ターンは短い。20–100ターンで口調が薄まるかは未知。
- **β / α adherence の自動スコアは未実装**。現状の人格維持評価は主観観測である。
- **θ の矛盾検出はヒューリスティック**である（禁止語 + 許可表現の素朴一致）。
- **δ の自動更新が弱い**。関係値は明示指定または実験側の手動更新に寄っている。
- **γ が実対話でまだ空**。申請閾値と対話長の設計が未調整。
- **生成器の実API未接続**。manual は実験純度が高いが運用には向かない。
- **口調検出は規則ベースではない**。逸脱を機械が指摘できない。
- **単一キャラ・単一対話者**。複数相手の δ 分離はデータ構造上あるが、実測していない。
- **二次創作ガイドライン遵守は運用側の責任**である。エンジンは公式を名乗らない制約を α に持つだけである。

過大な主張（汎用人格OS、幻覚の根絶、任意キャラの完全固定）は本ノートの範囲外である。

---

## 8. What Is Actually Novel

技術要素を個別に見れば、ハッシュ、レイヤ記憶、テンション数値、システムプロンプトはいずれも既知である。組み合わせとして新しいのは次の一文に尽きる。

> 人格をモデル内部の重みや長い会話履歴に預けず、  
> **検証可能な外部核として毎ターン再注入し、  
> 圧力は保存せず観測し、  
> 重要な記憶だけを人間が定着させる。**

「高性能なキャラクターモデル」ではなく、**モデルがどれだけ高性能でも核を外側から拘束できるか**を問うランタイムである。

---

## 9. Future Work

優先度は実験計画に従う。

1. **事後観測層（予定していた第3段）**  
   β adherence（name / tone / thinking_center / personality / core_values）と α adherence（rule / prohibition）の簡易スコア。  
   「Capsuleは壊れていない」と「LLMが従った」を分離する。

2. **長期軌道**  
   同一核で 30 ターン以上。Ζ の帯域変化と口調逸脱の同時記録。

3. **γ 昇格の実運用調整**  
   申請が出る対話長と閾値の再設定。空の γ を意図的に埋める実験。

4. **API generator の後付け**  
   抽象 `Callable[[str], str]` を崩さずに差し込む。焦点を接続成功に移さない。

5. **θ の構造的矛盾検出**  
   禁止の否定・許可の衝突をより硬い判定へ。

---

## 10. Conclusion

Double Capsule Memory は、変わってはいけないもの（α / β）と変わってよいもの（δ / γ / ε）をハッシュで分離し、圧力 Ζ を非保存の観測量として外に置いた。RoleplayEngine はその状態を毎ターン Control Prompt へ写し、LLM を「核の保管庫」ではなく「核に拘束された生成器」として使う。

実装済みの機械検証は 8/8 で通過した。短対話の manual 接続では Hash-A は不変で、核価値観と口調の維持が観察された。Ζ は制御信号として応答トーンに影響する兆候を示した。

現時点での強みは生成の上手さではない。**同一性を検証可能な外部状態として扱い、LLMの外側で人格を拘束できる最小カーネルになっていること**である。

---

## Appendix A. β 封入データ（実験核）

```
name: 霧雨魔理沙
tone: 砕けた男言葉。「〜だぜ」「〜だな」「〜ぜ」。丁寧語は基本使わない。
thinking_center: 魔法の研究と強さ。興味を持ったものはすぐに手に入れようとする。
personality: 負けず嫌い / 根は真っ直ぐだが表向きはひねくれ / 自信家 / 蒐集癖 / 努力は隠す
core_values: 派手でなければ魔法じゃない / 実力で勝つ / 努力はするが隠す
background: 魔法の森の人間魔法使い。霊夢とは長い付き合い。
```

Hash-A（本スナップショット）: `3052f7f955a48057…`

## Appendix B. 実行インターフェース

```
python3 roleplay_engine.py          # stub CLI
python3 roleplay_engine.py manual   # 制御プロンプトを外部LLMへ渡す
python3 roleplay_engine.py demo     # 固定シナリオ
```

CLI コマンド: `/state` `/prompt` `/mode stub|manual` `/approve` `/reject` `/sign +1|0|-1` `/quit`

## Appendix C. 用語

| 記号 | 意味 |
|------|------|
| α | 絶対制約 |
| β | 人格核 |
| θ | 追加制約（非 Hash-A） |
| δ | 関係状態 |
| γ | エピソード記憶 |
| ε | 作業記憶 |
| Ζ | 非保存テンション |
| Hash-A | α+β の同一性証明 |
| Hash-B | Outer の内容証明 |
| trusted | 最後に受理した Hash-B |
| Control Prompt | 毎ターンの拘束入力 |

---

*本ノートは実装と実行ログに忠実である。未測の一般化は主張しない。*
