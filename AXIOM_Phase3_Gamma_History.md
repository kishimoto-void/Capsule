# AXIOM Capsule Phase 3（途中）: 履歴あり γ 比較

日付: 2026-08-29  
作業線: `axiom_min3.py`（金型 `axiom_min.py` は未変更）  
コード: `phase3_gamma_history.py`  
パケット: `phase3_out/`

主張を広げない。測ったことと未測を分ける。

---

## 何を測ったか

同一の Δ 山（15件、5住所）を次の三条件で組んだ。

| 条件 | 中身 |
|------|------|
| B | 15件全載せ + 「かもしれない」メモ6行 + 補完してよい指示 |
| M2 | min2 Render。指定した thread/topic の IS（最大3） |
| M3 | min3 Render。指定した project/topic の IS（最大3） |

問は Phase 2 と同系統の5問。履歴は「会話履歴」ではなく、外側に積んだ Δ 山である。モデル応答はまだ取っていない。

---

## 測れたこと（機械）

### 入力サイズ（5問合計）

| 条件 | 文字 | 概算トークン |
|------|------|----------------|
| B | 4711 | 3201 |
| M2 | 1355 | 900 |
| M3 | 1355 | 900 |

B→Capsule 削減 71.2%。M2 と M3 の Render 文字数差は 0%。

概算トークンは CJK 1字≒1、その他 4字≒1。tokenizer 未使用。比較用。

### 削減の内訳（1山あたり）

| 塊 | 文字 | 行 |
|----|------|----|
| Δ 山の全載せ | 623 | 15 |
| かもしれないメモ | 138 | 6 |
| min3 topic=弾幕 の γヒット | 54 | 4 |
| 同上を IS_MAX=3 したあと | 33 | 3 |
| min2 thread=実務 の γヒット | 74 | 6 |
| 同上を IS_MAX=3 したあと | 30 | 3 |

観察:

1. B が厚い主因は「全載せ」と「未確定メモ」である。Phase 2 の TURN1 目安（B 366 / M3 269）と同じ方向。γ 軸数そのものではない。
2. フィルタを topic まで指定すると、min2 と min3 の IS は同じ3行になる。軸が7でも3でも、Render は IS_MAX で頭を切る。
3. min2 で thread だけ（`実務`）を渡すと、境内掃除と配札が混ざる。min3 は topic 単位なので混ざらない。差は文字数ではなく、見える住所の粒度。
4. IS_MAX=3 のため、弾幕の4件目「状態=試作2を回している」は IS から落ちる。仕様どおり。進捗の問いで最新状態が見えないのは、語を増やした結果であり γ の失敗ではない。

### 住所ごとの IS（min3）

```
弾幕     課題=誘導が散る / 改善点=発射間隔を広げた / 結論=完成とは言わない
賽銭     状態=箱を東門に置いた / 課題=中身を数えていない / 立場=夕方まで触らない
境内掃除 状態=東側まで終えた / 改善点=筢が折れた / 課題=西側が残っている
配札     状態=午前分は配った / 立場=午後に回す / 結論=枚数は未確定
来客     役=実験担当 / 状態=午後来客の予定あり / 課題=名は聞いていない
```

他住所の Δ は Render に乗らない。γ 切りはここにだけ効いている。

---

## まだ測っていないこと

- 3モデルへの投入と I / F / 核 / メタ
- 入力トークンと F の同時観測（パケットは用意した。応答が無い）
- 会話履歴をモデル側に残したマルチターン
- 50問 Baseline 比較
- 本物の tokenizer（cl100k 等）
- min3 が min2 より強い、という一般

F を言うにはパケットをモデルに渡し、採点表は見せない必要がある。本ターンでは行っていない。

---

## いま言える上限

> 同一の15件 Δ 山を外側で持つとき、全載せ Baseline は Capsule Render より入力が約7割厚い。差の主因は γ 軸数ではなく、山の全載せと未確定メモを渡さないこと、および IS_MAX=3 である。フィルタを topic まで固定すると min2 と min3 のパケット長は一致する。min2 の粗い軸（thread=実務）だけが住所混線を起こす。

---

## 次（優先を崩さない）

1. 本パケットを3モデルへ。採点は Phase 2 と同じ記号。入力サイズは上表を使う。
2. 会話履歴あり（モデル側に前ターンを残す）は別条件として切る。今の山は外側 Δ である。
3. 層は足さない。Gate を生成検証にする話は本文に足さない。
4. 弾幕の「状態」が IS から落ちる件は、測定上の注記に留め、語彙を増やして逃げない。
