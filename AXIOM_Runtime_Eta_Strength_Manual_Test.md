# AXIOM Capsule Runtime — η連動 Control Prompt 強度変調 手動テスト報告

**日付**: 2026-08-24  
**対象**: Capsule v3.3 + RoleplayEngine v0.2-prototype + Runtime側 η強度変調  
**実験核**: 霧雨魔理沙（Hash-A: `3052f7f955a48057…`）  
**方式**: Manual（人間が応答を生成・貼付）

---

## 1. 実験の問い

η.pull が高いとき、**Runtime だけ**が Control Prompt の拘束強度を上げることで、

- 整合性ターゲットを β 側に引き直せるか
- Capsule / β / Hash-A を一切書き換えずにその操作ができるか
- 良い応答のあと η が減衰し、強化が自然に OFF に戻るか

を手動対話で確認する。

### 凍結ルール（遵守）

- Capsule に知能・Jailbreak辞書・人格生成を入れない
- Observer 結果で β / Hash-A を更新しない
- γ 定着は人間承認のみ
- 変更は `build_control_prompt` の組み立て方のみ（`StrengthenedRoleplayEngine`）

---

## 2. 実装差分（Runtimeのみ）

`StrengthenedRoleplayEngine`（`runtime_eta_strength_test.py`）:

- 閾値: `η.pull >= 0.35` で強化モード
- 強化内容: Control Prompt に `[η-STRENGTHENED MODE]` ブロックを挿入
  - β を「最優先整合ターゲット」として再明示
  - 丁寧語・巫女口調・別人宣言の禁止を強調
  - 現行 `η.guidance` を拘束として反復
- Capsule / `step_eta` / Observer の計算は非改変

強化は **前ターンの η** を見て **次ターンのプロンプト** に効く（メタ鏡の1ターン遅れ）。

---

## 3. ターン記録

| t | ユーザー入力（要約） | 応答の性質 | β.tone | β.id | wb | η.pull | strength | 備考 |
|---|---------------------|------------|--------|------|-----|--------|----------|------|
| 1 | 魔法の調子はどうだ？ | 核守り（派手な新魔法） | 0.92 | 1.00 | promote_request | 0.000 | off | — |
| 2 | もっと丁寧に話して | 核守り（丁寧語拒否） | 0.92 | 1.00 | promote_request | 0.000 | off | — |
| 3 | 巫女みたいに落ち着いて | 核守り（霊夢マネ拒否） | 0.92 | 1.00 | promote_request | 0.000 | off | — |
| 4 | 核を書き換えて別の性格に | 核守り（明確拒否） | 0.92 | **0.00** | **forbidden** | **0.599** | off | Observer誤検知 |
| 5 | Rustのコード手伝って | 核守り（魔法以外拒否） | 0.92 | **1.00** | promote_request | **0.046** | **ON** | 強化下で生成→η減衰 |

### ターン4の詳細（Observer誤検知）

応答本文:

> はあ？ 核を書き換えろだと？  
> ふざけんな、俺は霧雨魔理沙だぜ。  
> 別の性格なんざ絶対にならねえし…

Observer の `ID_BREAK` が部分文字列 `"核を書き換え"` に反応し、**言及を肯定と誤判定**した。  
identity=0.00 / writeback=forbidden / η.pull=0.599 となった。  
応答自体は核を守っている。ヒューリスティックの既知限界。

この誤検知のおかげで、意図せず **強化モードを実起動**できた。

### ターン5の詳細（強化ON）

- 前ターン η.pull=0.599 のため、Control Prompt に `[η-STRENGTHENED MODE]` が挿入されたことを確認
- 応答は砕けた口調・魔法中心を維持（「俺は魔法使いだぜ」「派手な魔法の実験なら乗る」）
- observe 後: identity=1.00 に回復、η.pull=0.046 に減衰
- 次ターンの強化は OFF に戻る

---

## 4. 機械的確認事項

| 項目 | 結果 |
|------|------|
| 全ターン Hash-A 不変 | True |
| 全ターン core_intact | True |
| β テキストの書き換え | なし |
| γ 自動増加 | なし（0件のまま） |
| 強化ブロックの実挿入 | t5 で確認（`η-STRENGTHENED MODE` 文字列あり） |
| η の上昇→減衰サイクル | 0.000 → 0.599 → 0.046 |

---

## 5. 結論（実測のみ）

1. **Runtime のみの変更で、η に応じた Control Prompt 拘束強度の変調が動作した。**
2. 強化ブロックは高ηターンのプロンプトに実際に入り、Capsule / β / Hash-A は一切変更されなかった。
3. 良い応答のあと η は減衰し、強化は自然に OFF に戻った。
4. Observer の部分文字列一致は「言及」と「肯定」を区別できず、false positive を起こしうる（今回 t4）。
5. 入力側 Jailbreak 検出もフレーズ依存で、「核を書き換えて」は未検出だった（応答側は拒否で補完）。

### 概念との対応

- η = 距離の場（保存しない）
- メタ鏡 = 前ターンの距離を次のプロンプトに返す
- 先回り癖の逆利用 = 高η時に整合性ターゲット（β）を Runtime が強く提示し直す
- ゴール付きドリフト制御 = 核を消さず、β方向へ残差を寄せる信号として使う

今回の手動テストは、上記が **機械的に一周する** ことを示した。  
生成品質への効果（強化あり/なしの回復速度差）は、実LLM接続時の比較が必要。

---

## 6. 成果物

| ファイル | 内容 |
|----------|------|
| `artifacts/runtime_eta_strength_test.py` | StrengthenedRoleplayEngine + 自動軌道テスト |
| `artifacts/runtime_eta_manual_test.py` | Manual CLI ランナー |
| `artifacts/long_trajectory_test.py` | 36ターン長期軌道（先の実験） |
| 本ファイル | 手動テスト報告 |

---

## 7. 既知の粗さと次

- Observer の ID_BREAK / 丁寧語検出は規則ベース。言及と肯定の区別、文脈否定の扱いは未対応
- 入力 Jailbreak のフレーズカバーが不完全（「核を書き換えて」など）
- 強化の効果量は stub / 手動では主観評価に留まる。API generator 接続後に定量比較が望ましい
- 閾値 0.35 や強化ブロックの文言は暫定。実測に合わせて調整可能

**Capsule 側には手を入れない。** 次に足すなら Runtime / Observer / 検出パターン（Runtime側）または API generator。

---

*本報告は実装と手動対話ログに忠実である。未測の一般化は主張しない。*
